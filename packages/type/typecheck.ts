// tslint:disable

import { Style } from './';

const css: Style = {
  '@keyframes': {
    '': {},
  },
  animationName: {
    '': {},
  },
  ':hover': {},
};

// Avoid unread variables type error
css;
